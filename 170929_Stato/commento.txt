Per visualizzare lo stato dei thread durante l'esecuzione del programma ho implementato due interfacce: Observed ed Observer (Osservato e Osservatore) contenenti i metodi necessari a far sì che un terzo thread, creato appositamente per osservare lo stato dei thread in esecuzione, sia in grado di acquisire e stampare lo stato dei thread Downloader e Network ad ogni cambiamento di stato degli stessi. Il thread osservatore viene creato nella classe Observe, anch'essa creata appositamente per risolvere l'esercizio, e assume lo stato del thread attualmente in esecuzione, per poi stamparne lo stato nel caso sia "vivo" (isAlive).
